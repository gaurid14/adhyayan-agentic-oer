# Keep LF in repository + working tree for all text (good for Django templates)
* text=auto eol=lf

# Windows-specific scripts should stay CRLF
*.bat  text eol=crlf
*.cmd  text eol=crlf
*.ps1  text eol=crlf
